# cfg-optimizer
c code to optimized control flow graph (CFG) — A Clang/LLVM-based C code optimizer that extracts CFGs, performs data-flow analysis, and applies four compiler optimizations with visual output
